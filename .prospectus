item do
  name 'pubkeys::submodule'

  expected do
    url_xpath
    url 'https://github.com/akerl/keys'
    xpath '//a[@class="commit-tease-sha"]'
  end

  actual do
    git_hash
    dir 'keys'
  end
end
